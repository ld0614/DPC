name: Release to GitHub
on:
  workflow_dispatch:
    inputs:
      releaseType:
        description: 'Release Type'
        required: true
        default: 'Full Release'
        type: choice
        options:
        - Full Release
        - Release Candidate
        - Preview
        - Beta
      nextVersionType:
        description: 'Next Version Type'
        required: true
        default: 'Patch'
        type: choice
        options:
        - Breaking Change
        - Feature Update
        - Patch

env:
  productionRelease: ${{ (inputs.releaseType == 'Full Release' || inputs.releaseType == 'Release Candidate') && true || false }}
  preRelease: ${{ (inputs.releaseType == 'Full Release') && false || true }}

permissions:
  id-token: write
  contents: write
  actions: read

jobs:
  setup:
    runs-on: windows-latest
    outputs:
      productionRelease: ${{ env.productionRelease }}
      releaseNumber: ${{ steps.get-release-details.outputs.releaseNumber }}
      finalReleaseNumber: ${{ steps.get-release-details.outputs.finalReleaseNumber }}
      releaseName: ${{ steps.get-release-details.outputs.releaseName }}
      installerNumber: ${{ steps.get-release-details.outputs.installerNumber }}
      versionOverride: ${{ steps.get-release-details.outputs.versionOverride }}
      autoUpgradeWorking: ${{ steps.get-release-details.outputs.autoUpgradeWorking }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set Output Variables
        id: set-outputs
        run: |
          echo "Setting output variables:"
          echo "productionRelease=${{ env.productionRelease }}"

      - name: Get Release Details
        id: get-release-details
        shell: pwsh
        run: './DPCManagement/Scripts/Get-ReleaseNumber.ps1 -ReleaseType "${{ inputs.releaseType }}" -NextVersionType "${{ inputs.nextVersionType }}"'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Display Version Variables
        run: |
          echo "Version Number: ${{ steps.get-release-details.outputs.releaseNumber }}"
          echo "Final Release Number: ${{ steps.get-release-details.outputs.finalReleaseNumber }}"
          echo "Release Name: ${{ steps.get-release-details.outputs.releaseName }}"
          echo "Installer Number: ${{ steps.get-release-details.outputs.installerNumber }}"
          echo "Version Override: ${{ steps.get-release-details.outputs.versionOverride }}"
          echo "Auto Upgrade Working: ${{ steps.get-release-details.outputs.autoUpgradeWorking }}"

  build-dpc:
    needs: setup
    uses: ./.github/workflows/zz_module_BuildDPC.yml
    with:
      productionRelease: ${{ fromJSON(needs.setup.outputs.productionRelease) }}
      signRelease: false
      versionOverride: ${{ needs.setup.outputs.versionOverride }}
    secrets: inherit

  deploy:
    runs-on: windows-latest
    needs: [setup, build-dpc]

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get Release Notes
        id: get-release-notes
        shell: pwsh
        run: './DPCManagement/Scripts/Get-ReleaseNotes.ps1 -ReleaseType "${{ inputs.releaseType }}" -Version "${{ needs.setup.outputs.finalReleaseNumber }}" -InstallerVersion "${{ needs.setup.outputs.installerNumber }}" -AutoUpgradeWorking "${{ fromJSON(needs.setup.outputs.autoUpgradeWorking) }}"'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download artifact
        uses: actions/download-artifact@v8
        with:
          artifact-ids: ${{ needs.build-dpc.outputs.installerId }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Git Tag
        run: |
          git config user.name "GitHub Actions"
          git config user.email "github-actions@users.noreply.github.com"
          git tag ${{ needs.setup.outputs.releaseNumber }}
          git push origin ${{ needs.setup.outputs.releaseNumber }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
            prerelease: ${{ env.preRelease }}
            draft: true
            body: ${{ steps.get-release-notes.outputs.releaseText }}
            name: ${{ needs.setup.outputs.releaseName }}

            files: |
                DPC.msi